```markdown id="singly-linked-list-java"
# Singly Linked List Implementation in Java

## Linked List

A **Linked List** is a linear data structure where elements are stored in nodes.

Each node contains:

```

Data

*

Address of next node

```

Structure:

```

Node
|
|-- data
|
|-- next

```

Example:

```

10 → 20 → 30 → 40 → NULL

````

---

# Node Structure

```java
class Node {

    int data;
    Node next;


    Node(int value) {

        data = value;
        next = null;
    }
}
````

### Explanation

`data`

Stores the value of the node.

`next`

Stores the address/reference of the next node.

Example:

```
Node(10)

data = 10

next = next node address
```

---

# Singly Linked List Operations

## 1. Insert

* Insert at Beginning
* Insert at End
* Insert at Specific Position

## 2. Delete

* Delete at Beginning
* Delete at End
* Delete at Specific Position

## 3. Traversal

* Forward Traversal
* Reverse Traversal

---

# Java Implementation

```java
class Node {

    int data;
    Node next;


    Node(int value) {

        data = value;
        next = null;
    }
}


class LinkedList {


    Node head;



    // Insert at Beginning

    void insertAtBegin(int value) {

        Node newNode = new Node(value);


        newNode.next = head;


        head = newNode;
    }



    // Insert at End

    void insertAtEnd(int value) {

        Node newNode = new Node(value);


        if(head == null) {

            head = newNode;
            return;
        }


        Node temp = head;


        while(temp.next != null) {

            temp = temp.next;
        }


        temp.next = newNode;
    }




    // Insert at Specific Position

    void insertAtSpecific(int value, int position) {


        Node newNode = new Node(value);



        if(position == 1) {

            newNode.next = head;

            head = newNode;

            return;
        }



        Node temp = head;



        for(int i = 1; i < position-1 && temp != null; i++) {

            temp = temp.next;
        }



        if(temp == null) {

            System.out.println("Out of range");

            return;
        }



        newNode.next = temp.next;

        temp.next = newNode;
    }




    // Delete at Beginning

    void deleteAtBegin() {


        if(head == null) {

            System.out.println("NULL");

            return;
        }


        System.out.println("Deleted : " + head.data);


        head = head.next;
    }




    // Delete at End

    void deleteAtEnd() {


        if(head == null) {

            return;
        }



        if(head.next == null) {

            System.out.println("Deleted : " + head.data);

            head = null;

            return;
        }



        Node temp = head;



        while(temp.next.next != null) {

            temp = temp.next;
        }



        System.out.println("Deleted : " + temp.next.data);


        temp.next = null;
    }





    // Delete at Specific Position

    void deleteAtSpecific(int position) {


        if(head == null) {

            return;
        }



        if(position == 1) {

            System.out.println("Deleted : " + head.data);

            head = head.next;

            return;
        }



        Node temp = head;



        for(int i = 1; i < position-1 && temp != null; i++) {

            temp = temp.next;
        }



        if(temp == null || temp.next == null) {

            System.out.println("Out of range");

            return;
        }



        System.out.println("Deleted : " + temp.next.data);


        temp.next = temp.next.next;
    }





    // Forward Traversal

    void display() {


        Node temp = head;



        while(temp != null) {

            System.out.print(temp.data + " -> ");

            temp = temp.next;
        }


        System.out.println("NULL");
    }





    // Reverse Traversal Using Recursion

    void displayReverse(Node node) {


        if(node == null) {

            return;
        }


        displayReverse(node.next);


        System.out.print(node.data + " <- ");
    }



    void displayReverse() {

        displayReverse(head);

        System.out.println("NULL");
    }





    public static void main(String[] args) {


        LinkedList ll = new LinkedList();



        System.out.println("Insert At Beginning");


        ll.insertAtBegin(20);

        ll.insertAtBegin(10);


        ll.display();



        System.out.println("\nInsert At 3rd Position");


        ll.insertAtSpecific(30,3);


        ll.display();




        System.out.println("\nInsert At End");


        ll.insertAtEnd(40);


        ll.display();




        System.out.println("\nDelete At Beginning");


        ll.deleteAtBegin();


        ll.display();




        System.out.println("\nDelete At End");


        ll.deleteAtEnd();


        ll.display();




        System.out.println("\nDelete At 1st Position");


        ll.deleteAtSpecific(1);


        ll.display();




        System.out.println("\nReverse Traversal");


        ll.displayReverse();

    }
}
```

---

# Dry Run

## 1. Insert At Beginning

Initially:

```
head = NULL
```

Insert:

```
20
```

New node:

```
20 → NULL
```

head points to 20.

Insert:

```
10
```

New node points to old head.

```
10 → 20 → NULL
```

---

# 2. Insert At Specific Position

Current:

```
10 → 20 → NULL
```

Insert:

```
30

Position = 3
```

Before:

```
10 → 20 → NULL
```

Create node:

```
30
```

Connect:

```
20.next = 30
```

After:

```
10 → 20 → 30 → NULL
```

---

# 3. Insert At End

Insert:

```
40
```

Traverse till last node:

```
10 → 20 → 30 → NULL
              ↑
             last
```

Connect:

```
30.next = 40
```

Final:

```
10 → 20 → 30 → 40 → NULL
```

---

# 4. Delete At Beginning

Before:

```
10 → 20 → 30 → 40 → NULL
```

Move head:

```
head = head.next
```

After:

```
20 → 30 → 40 → NULL
```

Deleted:

```
10
```

---

# 5. Delete At End

Before:

```
20 → 30 → 40 → NULL
```

Find second last node:

```
30
```

Remove next:

```
30.next = NULL
```

After:

```
20 → 30 → NULL
```

Deleted:

```
40
```

---

# 6. Delete Specific Position

Delete position 1:

Before:

```
20 → 30 → NULL
```

Move head:

```
head = head.next
```

After:

```
30 → NULL
```

Deleted:

```
20
```

---

# Reverse Traversal

Using recursion.

Original:

```
10 → 20 → 30 → NULL
```

Recursive calls go forward:

```
10
20
30
```

Printing happens while returning:

```
30 ← 20 ← 10 ← NULL
```

---

# Complexity Analysis

Let:

```
n = number of nodes
```

---

## Insert At Beginning

Only change head.

Time:

```
O(1)
```

---

## Insert At End

Need to traverse till last node.

Time:

```
O(n)
```

---

## Insert Specific Position

Need traversal.

Time:

```
O(n)
```

---

## Delete Beginning

Move head.

Time:

```
O(1)
```

---

## Delete End

Find previous node.

Time:

```
O(n)
```

---

## Delete Specific Position

Traversal required.

Time:

```
O(n)
```

---

## Forward Traversal

Visit every node.

Time:

```
O(n)
```

---

## Reverse Traversal

Recursion visits every node.

Time:

```
O(n)
```

Space:

```
O(n)
```

because of recursion stack.

---

# Interview Important Points

## Singly Linked List Node

```java
class Node{

 int data;

 Node next;

}
```

---

## Insert Beginning Pattern

```java
newNode.next = head;

head = newNode;
```

---

## Delete Beginning Pattern

```java
head = head.next;
```

---

## Insert Middle Pattern

```java
newNode.next = temp.next;

temp.next = newNode;
```

---

## Delete Middle Pattern

```java
temp.next = temp.next.next;
```

