# 🔷 Mini Parser (LeetCode 385)
<img width="1024" height="1536" alt="image" src="https://github.com/user-attachments/assets/205a183c-88d1-4171-ad4f-d3ccc5f853c9" />

## 📌 Problem Statement

Given a string representing a nested list of integers, deserialize it into a NestedInteger object.

A NestedInteger can store:

1. A single integer

Example:

```
123
```

or

2. A nested list

Example:

```
[123,[456,[789]]]
```

---

# Example

## Input

```
"[123,[456,[789]]]"
```

## Output

```
[123,[456,[789]]]
```


Stores incomplete parent lists.

Example:

After reading:

```
[123,[456
```

Stack:

```
[
 [123],
 [456]
]
```

---

# Step-by-Step Dry Run

## Input

```
[123,[456,[789]]]
```

---

# Initial State

```
stack = []

curr = null

number start = 0
```

---

# 🔁 Step 1

Character:

```
[
```

Create new NestedInteger list.

Code:

```java
curr = new NestedInteger();
```

State:

```
stack = []

curr = []
```

---

# 🔁 Step 2

Characters:

```
123
```

Build number.

```
num = 123
```

When comma appears:

Add number.

```
curr.add(123)
```

State:

```
curr = [123]
```

---

# 🔁 Step 3

Character:

```
[
```

New nested list starts.

Before creating new list:

Save current list.

```java
stack.push(curr);
```

Stack:

```
[
 [123]
]
```

Create new list:

```
curr = []
```

---

# 🔁 Step 4

Characters:

```
456
```

Build number:

```
num = 456
```

Add:

```
curr.add(456)
```

State:

```
curr = [456]
```

Stack:

```
[
 [123]
]
```

---

# 🔁 Step 5

Character:

```
[
```

Again create nested list.

Push current list.

```
stack.push(curr)
```

Stack:

```
[
 [123],
 [456]
]
```

New list:

```
curr=[]
```

---

# 🔁 Step 6

Characters:

```
789
```

Build number:

```
num=789
```

Add:

```
curr.add(789)
```

State:

```
curr=[789]
```

---

# 🔁 Step 7

Character:

```
]
```

Current list completed.

Current:

```
[789]
```

Remove parent:

```java
parent = stack.pop();
```

Pop:

```
[456]
```

Attach:

```
parent.add(curr)
```

Now:

```
[456,[789]]
```

Current becomes:

```
curr=[456,[789]]
```

Stack:

```
[
 [123]
]
```

---

# 🔁 Step 8

Character:

```
]
```

Close:

```
[456,[789]]
```

Pop parent:

```
[123]
```

Attach:

```
[123,[456,[789]]]
```

Current:

```
[123,[456,[789]]]
```

Stack:

```
[]
```

---

# Final Output

```
[123,[456,[789]]]
```

---

# Java Implementation

```java
public NestedInteger deserialize(String s) {


    if(s.isEmpty())
        return null;


    // Case: Single Integer

    if(s.charAt(0) != '[')
        return new NestedInteger(Integer.valueOf(s));



    Stack<NestedInteger> stack = new Stack<>();

    NestedInteger curr = null;


    int start = 0;



    for(int i = 0; i < s.length(); i++){


        char ch = s.charAt(i);



        // Start new list

        if(ch == '['){


            if(curr != null){

                stack.push(curr);

            }


            curr = new NestedInteger();


            start = i + 1;

        }



        // End current list

        else if(ch == ']'){


            String num = s.substring(start,i);


            if(!num.isEmpty()){

                curr.add(
                    new NestedInteger(
                        Integer.valueOf(num)
                    )
                );

            }



            if(!stack.isEmpty()){


                NestedInteger parent = stack.pop();


                parent.add(curr);


                curr = parent;

            }


            start = i + 1;


        }



        // Number separator

        else if(ch == ','){



            if(s.charAt(i-1) != ']'){


                String num = s.substring(start,i);


                curr.add(
                    new NestedInteger(
                        Integer.valueOf(num)
                    )
                );

            }


            start = i + 1;

        }

    }


    return curr;

}
```


# Complexity Analysis

Let:

```
N = length of string
```

## Time Complexity

```
O(N)
```

Each character is processed once.

---

## Space Complexity

```
O(N)
```

