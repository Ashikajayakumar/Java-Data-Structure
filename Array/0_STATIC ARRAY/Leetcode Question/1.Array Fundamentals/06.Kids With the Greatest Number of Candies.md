# Kids With the Greatest Number of Candies (LeetCode 1431)
<img width="1402" height="1122" alt="image" src="https://github.com/user-attachments/assets/cad0ac06-470f-402e-ab55-dd9669b02bec" />
<img width="1024" height="1536" alt="image" src="https://github.com/user-attachments/assets/8b18a98c-8dec-49e8-ae77-ae7d3fbfcc8a" />

## Problem

You are given an array `candies` and an integer `extraCandies`.

Each kid has some candies. We want to check:

> If we give a kid all `extraCandies`, will they have the **greatest or equal greatest candies** among all kids?

Return a boolean array.

---

# Example

```text id="c1"
Input:
candies = [2,3,5,1,3]
extraCandies = 3
```

---

## Step 1: Find maximum candies

```text id="c2"
max = 5
```

---

## Step 2: Check each kid

### Kid 1

```text id="c3"
2 + 3 = 5 → true
```

### Kid 2

```text id="c4"
3 + 3 = 6 → true
```

### Kid 3

```text id="c5"
5 + 3 = 8 → true
```

### Kid 4

```text id="c6"
1 + 3 = 4 → false
```

### Kid 5

```text id="c7"
3 + 3 = 6 → true
```

---

## Output

```text id="c8"
[true, true, true, false, true]
```

---

# Visualization

```text id="c9"
candies = [2, 3, 5, 1, 3]
max = 5

Check:
2+3 = 5  ✔
3+3 = 6  ✔
5+3 = 8  ✔
1+3 = 4  ✘
3+3 = 6  ✔
```

---

# Main Idea

1. Find maximum in array
2. Check if:

```text id="c10"
candies[i] + extraCandies >= max
```

---

# Formula

candies[i] + extraCandies >=  max(candies)

---

# Java Solution

```java id="c11"
class Solution {

    public List<Boolean> kidsWithCandies(int[] candies, int extraCandies) {

        int max = 0;

        // Find maximum
        for(int candy : candies){
            max = Math.max(max, candy);
        }

        List<Boolean> result = new ArrayList<>();

        // Check each kid
        for(int candy : candies){

            if(candy + extraCandies >= max){
                result.add(true);
            } else {
                result.add(false);
            }
        }

        return result;
    }
}
```

---

# Dry Run

## Input

```text id="c12"
[2,3,5,1,3], extra = 3
```

### Max = 5

| Kid | Calculation | Result |
| --- | ----------- | ------ |
| 2   | 2+3=5       | true   |
| 3   | 3+3=6       | true   |
| 5   | 5+3=8       | true   |
| 1   | 1+3=4       | false  |
| 3   | 3+3=6       | true   |

---

umber of Digits
