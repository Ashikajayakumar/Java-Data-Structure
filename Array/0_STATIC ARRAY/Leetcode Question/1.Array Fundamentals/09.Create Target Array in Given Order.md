# Create Target Array in the Given Order (LeetCode 1389)

## Problem

You are given two arrays:

```text id="a1"
nums  = values
index = positions
```

You need to build a target array by inserting:

```text id="a2"
nums[i] at position index[i]
```

---

# Example

```text id="b1"
Input:
nums  = [0,1,2,3,4]
index = [0,1,2,2,1]
```

---

# Step-by-step building

Start:

```text id="c1"
[]
```

---

## i = 0

```text id="c2"
insert 0 at 0 → [0]
```

---

## i = 1

```text id="c3"
insert 1 at 1 → [0,1]
```

---

## i = 2

```text id="c4"
insert 2 at 2 → [0,1,2]
```

---

## i = 3

```text id="c5"
insert 3 at 2 → [0,1,3,2]
```

---

## i = 4

```text id="c6"
insert 4 at 1 → [0,4,1,3,2]
```

---

# Final Output

```text id="c7"
[0,4,1,3,2]
```

---

# Visualization

```text id="d1"
Step 1: []
Step 2: [0]
Step 3: [0,1]
Step 4: [0,1,2]
Step 5: [0,1,3,2]
Step 6: [0,4,1,3,2]
```

---

# Main Idea

We use:

```text id="e1"
ArrayList (dynamic insertion)
```

Because we need to insert at specific positions.

---

# Formula

target.insert(index[i],nums[i])

---

# Java Solution

```java id="f1"
class Solution {

    public int[] createTargetArray(int[] nums, int[] index) {

        List<Integer> list = new ArrayList<>();

        for(int i = 0; i < nums.length; i++) {

            list.add(index[i], nums[i]);
        }

        int[] result = new int[nums.length];

        for(int i = 0; i < nums.length; i++) {
            result[i] = list.get(i);
        }

        return result;
    }
}
```

---

# Dry Run

## Input

```text id="g1"
nums  = [0,1,2,3,4]
index = [0,1,2,2,1]
```

---

## Steps

### i = 0

```text id="g2"
[0]
```

### i = 1

```text id="g3"
[0,1]
```

### i = 2

```text id="g4"
[0,1,2]
```

### i = 3

```text id="g5"
[0,1,3,2]
```

### i = 4

```text id="g6"
[0,4,1,3,2]
```

