# LeetCode 179: Largest Number
<img width="1024" height="1536" alt="image" src="https://github.com/user-attachments/assets/daf55ece-160c-4a4a-8e1b-1433dd2c6592" />

## Problem Statement

Given a list of non-negative integers `nums`, arrange them such that they form the **largest number** and return it as a string.

---

## Example 1

```text
Input:
nums = [10,2]

Output:
"210"
```

---

## Example 2

```text
Input:
nums = [3,30,34,5,9]

Output:
"9534330"
```


# Simple Java Code

```java
class Solution {

    public String largestNumber(int[] nums) {

        String[] arr = new String[nums.length];

        for (int i = 0; i < nums.length; i++) {
            arr[i] = String.valueOf(nums[i]);
        }

        Arrays.sort(arr, (a, b) -> (b + a).compareTo(a + b));

        if (arr[0].equals("0")) {
            return "0";
        }

        StringBuilder sb = new StringBuilder();

        for (String s : arr) {
            sb.append(s);
        }

        return sb.toString();
    }
}
```

---

# Dry Run

```text
nums = [3,30,34,5,9]
```

Convert to String:

```text
["3","30","34","5","9"]
```

Sort using:

```text
(b+a) compareTo (a+b)
```

Comparisons:

```text
9 vs 5

95 > 59

9 first
```

```text
5 vs 34

534 > 345

5 first
```

Sorted:

```text
["9","5","34","3","30"]
```

Join:

```text
9534330
```

