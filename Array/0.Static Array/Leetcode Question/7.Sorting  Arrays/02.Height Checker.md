# LeetCode 1051: Height Checker

## Problem Statement

A school is trying to take an annual photo of all students.

The students should stand in **non-decreasing order of height**.

Given an integer array `heights`, return the number of indices where the current heights do not match the expected sorted order.

---

## Example

```text
Input:
heights = [1,1,4,2,1,3]

Output:
3
```

### Explanation

```text
Original:
[1,1,4,2,1,3]

Expected (Sorted):
[1,1,1,2,3,4]
```

Compare:

```text
Index 0 : 1 = 1 ✓
Index 1 : 1 = 1 ✓
Index 2 : 4 ≠ 1 ✗
Index 3 : 2 = 2 ✓
Index 4 : 1 ≠ 3 ✗
Index 5 : 3 ≠ 4 ✗
```

Count:

```text
3 mismatches
```

Answer:

```text
3
```

---

# Simple Java Code

```java
class Solution {

    public int heightChecker(int[] heights) {

        int[] expected = heights.clone();

        Arrays.sort(expected);

        int count = 0;

        for (int i = 0; i < heights.length; i++) {

            if (heights[i] != expected[i]) {
                count++;
            }
        }

        return count;
    }
}
```

---

# Dry Run

```text
heights = [1,1,4,2,1,3]

Copy Array

expected = [1,1,4,2,1,3]

Sort Copy

expected = [1,1,1,2,3,4]
```

Compare:

```text
1 = 1 ✓
1 = 1 ✓
4 ≠ 1 ✗
2 = 2 ✓
1 ≠ 3 ✗
3 ≠ 4 ✗
```

Count:

```text
3
```

---

# Visualization

```text
Original

[1,1,4,2,1,3]

        ↓ Sort Copy

[1,1,1,2,3,4]

Compare Position by Position

1 = 1 ✓
1 = 1 ✓
4 ≠ 1 ✗
2 = 2 ✓
1 ≠ 3 ✗
3 ≠ 4 ✗

Answer = 3
```

---

# Step-by-Step Flow

```text
Start
│
├── Copy Array
│
├── Sort Copy
│
├── Compare Elements
│
├── Count Differences
│
└── Return Count
```

---

# Complexity

| Complexity | Value      |
| ---------- | ---------- |
| Time       | O(n log n) |
| Space      | O(n)       |

---

# Interview Tip

The easiest approach is:

```text
1. Clone the array
2. Sort the cloned array
3. Compare original and sorted arrays
4. Count mismatched positions
```

This is the most common and accepted solution for **LeetCode 1051 – Height Checker**.

### One-Line Idea

```text
Sorted Copy + Compare = Answer
```
