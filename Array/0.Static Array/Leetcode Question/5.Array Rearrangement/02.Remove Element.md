# LeetCode 27: Remove Element

## Problem Statement

Given an integer array `nums` and an integer `val`, remove all occurrences of `val` in-place.

The relative order of the elements may be changed.

Return the number of elements in `nums` which are not equal to `val`.

---

## Example 1

```text
Input:
nums = [3,2,2,3]
val = 3

Output:
2

nums = [2,2,_,_]
```

### Explanation

```text
Remove all 3's.

Remaining elements:
[2,2]

Length = 2
```

---

## Example 2

```text
Input:
nums = [0,1,2,2,3,0,4,2]
val = 2

Output:
5

nums = [0,1,4,0,3,_,_,_]
```

### Explanation

```text
Remove all 2's.

Remaining elements:
[0,1,4,0,3]

Length = 5
```

---

# Approach (Two Pointers)

Use one pointer `k` to track the position where the next valid element should be placed.

### Steps

1. Traverse the array.
2. If `nums[i] != val`:

   * Place it at index `k`.
   * Increment `k`.
3. After traversal, the first `k` elements are the answer.

---

# Dry Run

```text
nums = [3,2,2,3]
val = 3

k = 0
```

### i = 0

```text
nums[0] = 3

Equals val

Skip
```

---

### i = 1

```text
nums[1] = 2

Copy to nums[k]
```

Array:

```text
[2,2,2,3]

k = 1
```

---

### i = 2

```text
nums[2] = 2

Copy to nums[k]
```

Array:

```text
[2,2,2,3]

k = 2
```

---

### i = 3

```text
nums[3] = 3

Skip
```

---

## Final Result

```text
Length = 2

nums = [2,2,_,_]
```

---

# Java Solution

```java
class Solution {

    public int removeElement(int[] nums, int val) {

        int k = 0;

        for (int i = 0; i < nums.length; i++) {

            if (nums[i] != val) {

                nums[k] = nums[i];
                k++;
            }
        }

        return k;
    }
}
```

---

# Time and Space Complexity

| Complexity | Value |
| ---------- | ----- |
| Time       | O(n)  |
| Space      | O(1)  |

---

# Simple Visualization

```text
nums = [3,2,2,3]
val = 3

k = 0

3 → Skip

2 → Keep
[2,_,_,_]

2 → Keep
[2,2,_,_]

3 → Skip

Result:
[2,2]
Length = 2
```

---

# Pointer Movement Visualization

```text
nums = [3,2,2,3]
        ↑

3 == val
Skip

nums = [3,2,2,3]
          ↑
k = 0

2 != val
Copy to k

nums = [2,2,2,3]
          ↑
k = 1

2 != val
Copy to k

nums = [2,2,2,3]
            ↑
k = 2

3 == val
Skip

Result:
[2,2]
```

---
    [2,2]
```
