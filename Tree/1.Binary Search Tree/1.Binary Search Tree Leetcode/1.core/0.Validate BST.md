 # Validate Binary Search Tree (LeetCode 98)            
   <img width="1024" height="1536" alt="image" src="https://github.com/user-attachments/assets/a1e4862d-145f-4f7c-8156-e10221b1e6c4" />    
    
## Problem                
              
Given the root of a binary tree, determine whether it is a valid BST.  
       
### BST Rule   

For every node: 
 
```text
Left Subtree < Root < Right Subtree
```

And this rule must hold for **all descendants**, not just immediate children.

---

## Example 1

```text
    2
   / \
  1   3
```

Output:

```text
true
```

Because:

```text
1 < 2 < 3
```

---

## Example 2

```text
      5
     / \
    1   4
       / \
      3   6
```

Output:

```text
false
```

Why?

Node `3` is in the right subtree of `5`.

Therefore it must be greater than `5`.

But:

```text
3 < 5
```

So this is not a BST.

---

# Wrong Approach

Many beginners do:

```java
if(root.left.val < root.val &&
   root.right.val > root.val)
```

This checks only immediate children.

It fails for:

```text
      10
     /  \
    5    15
        /  \
       6   20
```

Here:

```text
6 < 15
```

looks correct.

But:

```text
6 < 10
```

which violates BST rules.

---

# Correct Idea

Every node must stay within a valid range.

For example:

```text
      10
     /  \
    5    15
```

* Left subtree range: (-∞,10)
* Right subtree range: (10,+∞)

Pass these limits during DFS.

---

## Recursive Solution

```java
class Solution {

    public boolean isValidBST(TreeNode root) {
        return validate(root, Long.MIN_VALUE, Long.MAX_VALUE);
    }

    private boolean validate(TreeNode node, long min, long max) {

        if (node == null) {
            return true;
        }

        if (node.val <= min || node.val >= max) {
            return false;
        }

        return validate(node.left, min, node.val)
            && validate(node.right, node.val, max);
    }
}
```

---

# Dry Run

Tree:

```text
        5
       / \
      3   7
     / \   \
    2   4   8
```

Initial call:

```java
validate(5, -∞, +∞)
```

---

### Node 5

Check:

```text
-∞ < 5 < +∞
```

Valid.

Left:

```java
validate(3, -∞, 5)
```

Right:

```java
validate(7, 5, +∞)
```

---

### Node 3

Check:

```text
-∞ < 3 < 5
```

Valid.

---

### Node 2

Check:

```text
-∞ < 2 < 3
```

Valid.

---

### Node 4

Check:

```text
3 < 4 < 5
```

Valid.

---

### Node 7

Check:

```text
5 < 7 < +∞
```

Valid.

---

### Node 8

Check:

```text
7 < 8 < +∞
```

Valid.

All nodes satisfy the range.

Return:

```text
true
```

---

# Violation Example

```text
      10
     /  \
    5    15
        /
       6
```

Call:

```java
validate(6, 10, 15)
```

Check:

```text
10 < 6 < 15
```

False.

Return:

```text
false
```
