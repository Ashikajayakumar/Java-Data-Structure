# 105. Construct Binary Tree from Preorder and Inorder Traversal
<img width="1024" height="1536" alt="image" src="https://github.com/user-attachments/assets/47115088-a76d-425f-8895-25fba4e88646" />

## Problem

Given two integer arrays:

* **Preorder** = Root → Left → Right
* **Inorder** = Left → Root → Right

Construct and return the binary tree.

### Example

```text
preorder = [3,9,20,15,7]
inorder  = [9,3,15,20,7]
```

Output:

```text
      3
     / \
    9   20
       /  \
      15   7
```

---

# Key Idea

### Preorder

```text
Root Left Right
```

The first element is always the root.

```text
[3,9,20,15,7]
 ^
Root
```

### Inorder

```text
Left Root Right
```

Find the root in inorder.

```text
[9,3,15,20,7]
   ^
 Root
```

Everything:

* Left of root → Left subtree
* Right of root → Right subtree



# Optimized Java Solution (O(n))

```java
class Solution {

    int preIndex = 0;
    HashMap<Integer, Integer> map = new HashMap<>();

    public TreeNode buildTree(int[] preorder, int[] inorder) {

        for (int i = 0; i < inorder.length; i++) {
            map.put(inorder[i], i);
        }

        return build(preorder, 0, inorder.length - 1);
    }

    private TreeNode build(int[] preorder,
                           int start,
                           int end) {

        if (start > end)
            return null;

        int val = preorder[preIndex++];

        TreeNode root = new TreeNode(val);

        int pos = map.get(val);

        root.left = build(preorder, start, pos - 1);

        root.right = build(preorder, pos + 1, end);

        return root;
    }
}
```

---

# Dry Run

### Input

```text
preorder = [3,9,20,15,7]
inorder  = [9,3,15,20,7]
```

---

### Step 1

Root from preorder:

```text
3
```

Find in inorder:

```text
[9,3,15,20,7]
   ^
```

Split:

```text
Left  = [9]
Right = [15,20,7]
```

Tree:

```text
    3
```

---

### Step 2

Next preorder element:

```text
9
```

Left child:

```text
    3
   /
  9
```

---

### Step 3

Next preorder element:

```text
20
```

Right child:

```text
    3
   / \
  9  20
```

---

### Step 4

Next preorder element:

```text
15
```

Left child of 20:

```text
      3
     / \
    9  20
       /
      15
```

---

### Step 5

Next preorder element:

```text
7
```

Right child of 20:

```text
      3
     / \
    9   20
       /  \
      15   7
```


```
