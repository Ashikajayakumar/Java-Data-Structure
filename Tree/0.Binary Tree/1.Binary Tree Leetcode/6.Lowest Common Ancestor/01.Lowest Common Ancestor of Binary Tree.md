# 236. Lowest Common Ancestor of a Binary Tree

## Problem

Given a binary tree and two nodes `p` and `q`, return their **Lowest Common Ancestor (LCA)**.

### Definition

The **Lowest Common Ancestor** is the lowest node in the tree that has both `p` and `q` as descendants.

---

## Example 1

```text
        3
      /   \
     5     1
    / \   / \
   6   2 0   8
      / \
     7   4
```

```text
p = 5
q = 1
```

Answer:

```text
3
```

Because both nodes are found in different subtrees of 3.

---

## Example 2

```text
        3
      /   \
     5     1
    / \
   6   2
      / \
     7   4
```

```text
p = 5
q = 4
```

Answer:

```text
5
```

Because 5 itself is an ancestor of 4.

---

# Key Observation

At every node:

1. Search in left subtree.
2. Search in right subtree.

Three possibilities:

### Case 1

Both found in left subtree.

```text
      3
     /
    5
   / \
  p   q
```

Return left result.

---

### Case 2

Both found in right subtree.

```text
      3
       \
        1
       / \
      p   q
```

Return right result.

---

### Case 3

One found on left and one on right.

```text
        3
      /   \
     p     q
```

Current node is LCA.

Return current node.

---

# Recursive Idea

For every node:

```java
left = dfs(left subtree)
right = dfs(right subtree)
```

If:

```java
left != null && right != null
```

Current node is LCA.

---

# Java Solution

```java
class Solution {

    public TreeNode lowestCommonAncestor(TreeNode root,
                                         TreeNode p,
                                         TreeNode q) {

        if (root == null)
            return null;

        if (root == p || root == q)
            return root;

        TreeNode left =
                lowestCommonAncestor(root.left, p, q);

        TreeNode right =
                lowestCommonAncestor(root.right, p, q);

        if (left != null && right != null)
            return root;

        return left != null ? left : right;
    }
}
```

---

# Dry Run

```text
        3
      /   \
     5     1
    / \   / \
   6   2 0   8
      / \
     7   4

p = 7
q = 4
```

---

### Node 7

```java
root == p
```

Return:

```text
7
```

---

### Node 4

```java
root == q
```

Return:

```text
4
```

---

### Node 2

Receives:

```text
left = 7
right = 4
```

Both non-null:

```java
return 2;
```

Node 2 becomes LCA.

---

### Node 5

Receives:

```text
left = null
right = 2
```

Return:

```text
2
```

---

### Node 3

Receives:

```text
left = 2
right = null
```

Return:

```text
2
```

Final Answer:

```text
2
```
Let's dry-run the exact code for:

```text
root = [3,5,1,6,2,0,8,null,null,7,4]
p = 5
q = 4
```

Tree:

```text
          3
        /   \
       5     1
      / \   / \
     6   2 0   8
        / \
       7   4
```

---

# Initial Call

```java
lowestCommonAncestor(3,5,4)
```

Current node:

```text
3
```

Check:

```java
if(root == null)
```

No.

```java
if(root == p || root == q)
```

```text
3 != 5
3 != 4
```

No.

Now search both sides:

```java
left = lowestCommonAncestor(5,5,4);
right = lowestCommonAncestor(1,5,4);
```

---

# Left Recursive Call

```java
lowestCommonAncestor(5,5,4)
```

Current node:

```text
5
```

Check:

```java
if(root == p || root == q)
```

```text
5 == p
```

True.

Immediately return:

```java
return root;
```

Returns:

```text
5
```

So:

```java
left = 5
```

Notice something important:

### Recursion Stops Here

We do NOT visit:

```text
6
2
7
4
```

because as soon as we find `p`, we return.

---

# Right Recursive Call

```java
lowestCommonAncestor(1,5,4)
```

Current node:

```text
1
```

Check:

```text
1 != 5
1 != 4
```

Continue.

---

## Search Left of 1

```java
lowestCommonAncestor(0,5,4)
```

Current node:

```text
0
```

Not p or q.

Search children.

---

### Left of 0

```java
lowestCommonAncestor(null,5,4)
```

Returns:

```text
null
```

---

### Right of 0

```java
lowestCommonAncestor(null,5,4)
```

Returns:

```text
null
```

Now:

```java
left = null
right = null
```

Return:

```text
null
```

---

## Search Right of 1

```java
lowestCommonAncestor(8,5,4)
```

Current node:

```text
8
```

Not p or q.

Search children.

---

### Left of 8

```java
lowestCommonAncestor(null,5,4)
```

Returns:

```text
null
```

---

### Right of 8

```java
lowestCommonAncestor(null,5,4)
```

Returns:

```text
null
```

Return:

```text
null
```

---

Back at node 1:

```java
left = null
right = null
```

Check:

```java
if(left != null && right != null)
```

False.

Return:

```java
left != null ? left : right
```

Returns:

```text
null
```

So:

```java
right = null
```

---

# Back at Root 3

Now we have:

```java
left = 5
right = null
```

Check:

```java
if(left != null && right != null)
```

```text
5 != null ✓
null != null ✗
```

False.

Execute:

```java
return left != null ? left : right;
```

Returns:

```text
5
```

---

# Final Answer

```text
LCA = 5
```


