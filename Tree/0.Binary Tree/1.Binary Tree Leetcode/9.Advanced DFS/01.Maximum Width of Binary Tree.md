# LeetCode 662 — Maximum Width of Binary Tree (Medium)
<img width="1024" height="1536" alt="image" src="https://github.com/user-attachments/assets/443e63d7-7dbf-4e2c-aa50-eed34b847033" />

## Problem

Given the root of a binary tree, return the **maximum width** of the tree.

The width of one level is:

```text
(rightmost position - leftmost position + 1)
```

including the null nodes between them.

---

## Example 1

```text
        1
       / \
      3   2
     / \   \
    5   3   9
```

Output:

```java
4
```

Width at level 3:

```text
5   3   N   9
```

Positions:

```text
0   1   2   3
```

Width:

```text
3 - 0 + 1 = 4
```

---

## Key Idea

Assign an index to every node like a complete binary tree.

For a node at index `i`:

```text
Left Child  = 2 * i + 1
Right Child = 2 * i + 2
```

Example:

```text
          1(0)
         /    \
      3(1)    2(2)
      / \       \
   5(3) 3(4)   9(6)
```

Notice:

```text
Level 3 indices = [3,4,6]
Width = 6 - 3 + 1 = 4
```





---

## Java Code

```java
class Solution {

    class Pair {
        TreeNode node;
        long idx;

        Pair(TreeNode node, long idx) {
            this.node = node;
            this.idx = idx;
        }
    }

    public int widthOfBinaryTree(TreeNode root) {

        if(root == null) {
            return 0;
        }

        long ans = 0;

        Queue<Pair> q = new LinkedList<>();
        q.offer(new Pair(root, 0));

        while(!q.isEmpty()) {

            int size = q.size();

            long min = q.peek().idx;

            long first = 0;
            long last = 0;

            for(int i = 0; i < size; i++) {

                Pair curr = q.poll();

                long idx = curr.idx - min;

                if(i == 0) {
                    first = idx;
                }

                if(i == size - 1) {
                    last = idx;
                }

                if(curr.node.left != null) {
                    q.offer(
                        new Pair(curr.node.left,
                                 2 * idx + 1)
                    );
                }

                if(curr.node.right != null) {
                    q.offer(
                        new Pair(curr.node.right,
                                 2 * idx + 2)
                    );
                }
            }

            ans = Math.max(ans, last - first + 1);
        }

        return (int)ans;
    }
}
```

---

# Dry Run

Tree:

```text
          1
         / \
        3   2
       / \   \
      5   3   9
```

---

## Level 1

Queue:

```text
[(1,0)]
```

```java
first = 0
last = 0
```

Width:

```text
0 - 0 + 1 = 1
```

Answer:

```text
1
```

Add:

```text
3(1), 2(2)
```

---

## Level 2

Queue:

```text
[(3,1),(2,2)]
```

Normalize:

```java
min = 1
```

Indices become:

```text
0,1
```

Width:

```text
1 - 0 + 1 = 2
```

Answer:

```text
2
```

Add:

```text
5(1),3(2),9(4)
```

---

## Level 3

Queue:

```text
[(5,1),(3,2),(9,4)]
```

Normalize:

```java
min = 1
```

Indices:

```text
0,1,3
```

Width:

```text
3 - 0 + 1 = 4
```

Answer:

```text
4
```



