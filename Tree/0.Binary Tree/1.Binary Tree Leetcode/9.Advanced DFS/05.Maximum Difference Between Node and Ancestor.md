## LeetCode 1026: Maximum Difference Between Node and Ancestor

## Problem
 
Find the maximum value of: 
 
```text  
|ancestor.val - node.val|
```

where the ancestor is any node on the path from the root to the current node. 



---

## Simple DFS Solution

```java
class Solution {

    public int maxAncestorDiff(TreeNode root) {
        return dfs(root, root.val, root.val);
    }

    private int dfs(TreeNode node, int min, int max) {

        if (node == null) {
            return max - min;
        }

        min = Math.min(min, node.val);
        max = Math.max(max, node.val);

        int left = dfs(node.left, min, max);
        int right = dfs(node.right, min, max);

        return Math.max(left, right);
    }
}
```

---

# Step-by-Step Dry Run

Example:

```text
         8
       /   \
      3     10
     / \
    1   6
```

Start:

```java
dfs(8, 8, 8)
```

---

## Call 1

Node = 8

```text
min = 8
max = 8
```

Go left:

```java
dfs(3, 8, 8)
```

---

## Call 2

Node = 3

Update:

```text
min = min(8,3) = 3
max = max(8,3) = 8
```

Current path:

```text
8 → 3
```

Go left:

```java
dfs(1,3,8)
```

---

## Call 3

Node = 1

Update:

```text
min = 1
max = 8
```

Current path:

```text
8 → 3 → 1
```

Go left:

```java
dfs(null,1,8)
```

---

## Call 4

Node = null

Return:

```java
8 - 1 = 7
```

This means:

```text
Maximum difference on path
8 → 3 → 1
is 7
```

---

## Backtrack

Now:

```java
dfs(null,1,8)
```

for right child also returns:

```java
7
```

Node 1 returns:

```java
max(7,7) = 7
```

---

## Move to Node 6

Call:

```java
dfs(6,3,8)
```

Update:

```text
min = 3
max = 8
```

Path:

```text
8 → 3 → 6
```

At null children:

```java
8 - 3 = 5
```

Node 6 returns:

```java
5
```

---

## Back at Node 3

Left subtree:

```text
7
```

Right subtree:

```text
5
```

Return:

```java
max(7,5)=7
```

---

## Move to Node 10

Call:

```java
dfs(10,8,8)
```

Update:

```text
min = 8
max = 10
```

At null children:

```java
10 - 8 = 2
```

Return:

```text
2
```

---

## Root Decision

Left subtree returned:

```text
7
```

Right subtree returned:

```text
2
```

Answer:

```java
max(7,2)=7
```

---

# Visualization

```text
Path 1:
8 → 3 → 1

min = 1
max = 8

difference = 7
```

```text
Path 2:
8 → 3 → 6

min = 3
max = 8

difference = 5
```

```text
Path 3:
8 → 10

min = 8
max = 10

difference = 2
```

Maximum:

```text
7
```


The main idea is: **carry useful path state during DFS instead of recomputing it later.**
