# LeetCode 113: Path Sum II

## Problem Statement

Given the root of a binary tree and an integer `targetSum`, return **all root-to-leaf paths** where the sum of the node values in the path equals `targetSum`.

A leaf is a node with no left and right child.

---

## Example

![Image](https://images.openai.com/static-rsc-4/HP11i3G308WxAN42iO3qeonw7HfVfydxNiyiCsddsJLZwN7OcWKFFwufF1qo_ulF0JUQNs4I5iGBTNCoGcxpPj7u-ECOci_Uh7XJHHMWocMHSLnNoFSu9uEoAfw8WF8Gr4JbfZZ13epsV16P0Ag1_qPpjUDt2e7bhDZFDAmFpiHf_gjvo3Ep1tPyWP2XqtVy?purpose=fullsize)
<img width="1024" height="1536" alt="image" src="https://github.com/user-attachments/assets/762ca42a-9812-41a5-9b3d-e3fbe3cb80b6" />

```text
Input:

          5
         / \
        4   8
       /   / \
      11  13  4
     / \     / \
    7   2   5   1

targetSum = 22

Output:

[
 [5,4,11,2],
 [5,8,4,5]
]
```

# Dry Run

### Initial

```text
target = 22
path = []
```

---

### Visit 5

```text
path = [5]
remaining = 22 - 5 = 17
```

---

### Visit 4

```text
path = [5,4]
remaining = 17 - 4 = 13
```

---

### Visit 11

```text
path = [5,4,11]
remaining = 13 - 11 = 2
```

---

### Visit 7

```text
path = [5,4,11,7]
remaining = 2 - 7 = -5
```

Leaf but remaining ≠ 0

Backtrack:

```text
path = [5,4,11]
```

---

### Visit 2

```text
path = [5,4,11,2]
remaining = 2 - 2 = 0
```

Leaf and remaining = 0

Add:

```text
[5,4,11,2]
```

---

### Explore Right Side

```text
5 → 8 → 4 → 5
```

```text
5 + 8 + 4 + 5 = 22
```

Add:

```text
[5,8,4,5]
```

---

# Java Solution

```java
class Solution {

    public List<List<Integer>> pathSum(TreeNode root, int targetSum) {

        List<List<Integer>> result = new ArrayList<>();

        dfs(root, targetSum, new ArrayList<>(), result);

        return result;
    }

    private void dfs(TreeNode node,
                     int target,
                     List<Integer> path,
                     List<List<Integer>> result) {

        if (node == null) {
            return;
        }

        path.add(node.val);

        target -= node.val;

        if (node.left == null &&
            node.right == null &&
            target == 0) {

            result.add(new ArrayList<>(path));
        }

        dfs(node.left, target, path, result);
        dfs(node.right, target, path, result);

        path.remove(path.size() - 1);
    }
}
```

---

# Backtracking Visualization

```text
dfs(5,22)

path=[5]

├── dfs(4,17)
│
│   path=[5,4]
│
│   └── dfs(11,13)
│
│       path=[5,4,11]
│
│       ├── dfs(7,2)
│       │
│       │   path=[5,4,11,7]
│       │   Invalid
│       │
│       └── dfs(2,2)
│
│           path=[5,4,11,2]
│           Valid
│
└── dfs(8,17)
    ...
```

---

# Why Backtracking?

Suppose:

```text
Current path = [5,4,11,2]
```

After storing it:

```java
path.remove(path.size() - 1);
```

becomes

```text
[5,4,11]
```

This allows DFS to explore other branches without carrying extra nodes.

---

\
