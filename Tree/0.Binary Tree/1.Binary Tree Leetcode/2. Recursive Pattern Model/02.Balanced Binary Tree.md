# Balanced Binary Tree (LeetCode 110)   

## Problem 
<img width="1402" height="1122" alt="image" src="https://github.com/user-attachments/assets/23085ec1-e6ee-4d44-a303-9c67be1bdc27" />
<img width="1024" height="1536" alt="image" src="https://github.com/user-attachments/assets/b8d3729a-90bd-4c07-bc0d-f66a80eebfe6" />

Check whether a binary tree is height-balanced. 

A binary tree is balanced if:

 Difference between left subtree height and right subtree height is not more than 1 for every node.

---

# Balanced Tree Example



```text id="7mql2x"
        3
       / \
      9  20
         / \
        15  7
```

Output:

```text id="x8m4ql"
true
```

Because height difference everywhere ≤ 1.

---

# Unbalanced Tree Example

```text id="9mql4x"
        1
       /
      2
     /
    3
```

Output:

```text id="0mql8z"
false
```

Because:

```text id="1xpl9m"
Left height = 2
Right height = 0

Difference = 2
```

---


# Balance Formula
```
|Height(left) - Height(right)| ≤ 1
```
---

# Easy Java Solution

```java id="4mql8x"
class Solution {

    public boolean isBalanced(TreeNode root) {

        if(root == null){
            return true;
        }

        int left = height(root.left);
        int right = height(root.right);

        // Check current node
        if(Math.abs(left - right) > 1){
            return false;
        }

        // Check subtrees
        return isBalanced(root.left) &&
               isBalanced(root.right);
    }

    public int height(TreeNode root){

        if(root == null){
            return 0;
        }

        int left = height(root.left);
        int right = height(root.right);

        return Math.max(left, right) + 1;
    }
}
```

---

# Dry Run

## Tree

```text id="6xpl4m"
        1
       / \
      2   3
```

---

## Heights

### Node 2

```text id="k9m1ql"
height = 1
```

### Node 3

```text id="7m2qlx"
height = 1
```

### Root

```text id="p9m1ql"
|1 - 1| = 0
```
