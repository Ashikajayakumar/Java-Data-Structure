<img width="1536" height="1024" alt="image" src="https://github.com/user-attachments/assets/e4678e1f-30cc-4659-9cbf-de10f9488abb" />
<img width="1024" height="1536" alt="image" src="https://github.com/user-attachments/assets/a7f80c7b-dee9-4931-97d9-09c189e98f1b" />  
          
---        
# 🔷 1971. Find if Path Exists in Graph    

There is a bi-directional graph with n vertices, where each vertex is labeled from 0 to n - 1 (inclusive). 
The edges in the graph are represented as a 2D integer array edges, where each edges[i] = [ui, vi] denotes a bi-directional edge between vertex ui and vertex vi.
Every vertex pair is connected by at most one edge, and no vertex has an edge to itself.
You want to determine if there is a valid path that exists from vertex source to vertex destination. 
Given edges and the integers n, source, and destination, return true if there is a valid path from source to destination, or false otherwise. 

Example 1:
Input: n = 3, edges = [[0,1],[1,2],[2,0]], source = 0, destination = 2
Output: true
Explanation: There are two paths from vertex 0 to vertex 2:
- 0 → 1 → 2
- 0 → 2

----

# 🔷 1. First Understand the Problem

Suppose the input is:

```text
n = 5

edges = [
    [0,1],
    [0,2],
    [1,3],
    [2,4]
]

start = 0
destination = 4
```

The graph looks like:

```text
        0
       / \
      1   2
      |   |
      3   4
```

We need to answer:

```text
Can we go from 0 to 4?
```

Yes:

```text
0 → 2 → 4
```

So the answer should be:

```text
true
```

---

# 🔷 2. Complete Code

```java
class Solution { 

    public boolean validPath(int n, int[][] edges, int start, int destination) { 

        List<List<Integer>> list = new ArrayList<>(); 

        for(int i = 0; i < n; i++){ 
            list.add(new ArrayList<>()); 
        } 

        for(int[] edge : edges){ 

            int v = edge[0]; 
            int u = edge[1]; 

            list.get(v).add(u); 
            list.get(u).add(v); 
        } 

        Queue<Integer> q = new LinkedList<>(); 

        boolean[] visit = new boolean[n]; 

        q.add(start); 
        visit[start] = true; 

        while(!q.isEmpty()){ 

            int node = q.poll(); 

            if(node == destination)
                return true; 

            for(int nei : list.get(node)){ 

                if(!visit[nei]){ 

                    visit[nei] = true; 
                    q.add(nei); 
                } 
            } 
        } 

        return false; 
    } 
}
```

Now let's execute **every line**.

---

# 🔷 3. Step 1 — Create Adjacency List

We start with:

```java
List<List<Integer>> list = new ArrayList<>();
```

Think of this as:

```text
list
 ↓
0 → ?
1 → ?
2 → ?
3 → ?
4 → ?
```

We need one list for every node.

---

# 🔷 4. Create Empty List for Every Node

```java
for(int i = 0; i < n; i++){
    list.add(new ArrayList<>());
}
```

Here:

```text
n = 5
```

### Iteration 1

```text
i = 0
```

Create list for node `0`.

```text
0 → []
```

### Iteration 2

```text
i = 1
```

```text
0 → []
1 → []
```

### Iteration 3

```text
i = 2
```

```text
0 → []
1 → []
2 → []
```

### Iteration 4

```text
i = 3
```

```text
0 → []
1 → []
2 → []
3 → []
```

### Iteration 5

```text
i = 4
```

```text
0 → []
1 → []
2 → []
3 → []
4 → []
```

Loop finishes.

---

# 🔷 5. Add Edges

Our input:

```text
[0,1]
[0,2]
[1,3]
[2,4]
```

The code:

```java
for(int[] edge : edges){
```

means:

> Take one edge at a time.

---

## 🟢 Edge 1: `[0,1]`

```java
int v = edge[0];
int u = edge[1];
```

Therefore:

```text
v = 0
u = 1
```

Then:

```java
list.get(v).add(u);
```

means:

```text
list.get(0).add(1)
```

So:

```text
0 → [1]
```

Then:

```java
list.get(u).add(v);
```

means:

```text
list.get(1).add(0)
```

So:

```text
1 → [0]
```

Because this is an **undirected graph**, we add both directions.

```text
0 → [1]
1 → [0]
```

---

# 🟢 Edge 2: `[0,2]`

```text
v = 0
u = 2
```

Add:

```text
0 → [1, 2]
2 → [0]
```

Now:

```text
0 → [1, 2]
1 → [0]
2 → [0]
3 → []
4 → []
```

---

# 🟢 Edge 3: `[1,3]`

```text
v = 1
u = 3
```

Add:

```text
1 → [0, 3]
3 → [1]
```

Now:

```text
0 → [1, 2]
1 → [0, 3]
2 → [0]
3 → [1]
4 → []
```

---

# 🟢 Edge 4: `[2,4]`

```text
v = 2
u = 4
```

Add:

```text
2 → [0, 4]
4 → [2]
```

Final adjacency list:

```text
0 → [1, 2]
1 → [0, 3]
2 → [0, 4]
3 → [1]
4 → [2]
```

Graph:

```text
        0
       / \
      1   2
      |   |
      3   4
```

---

# 🔷 6. Create Queue

```java
Queue<Integer> q = new LinkedList<>();
```

Initially:

```text
Queue = []
```

A queue follows:

```text
FIFO

First In → First Out
```

Think of a line of people.

```text
Person A → Person B → Person C
   ↑
First person leaves first
```

---

# 🔷 7. Create Visited Array

```java
boolean[] visit = new boolean[n];
```

Since:

```text
n = 5
```

Initially:

```text
visit = [false, false, false, false, false]
```

Meaning:

```text
Node    Visited?
----------------
0       false
1       false
2       false
3       false
4       false
```

Nobody has been visited yet.

---

# 🔷 8. Add Start Node

Our:

```text
start = 0
```

Execute:

```java
q.add(start);
```

Queue becomes:

```text
Queue = [0]
```

Then:

```java
visit[start] = true;
```

Therefore:

```text
visit[0] = true
```

Now:

```text
visit = [true, false, false, false, false]
```

Meaning:

```text
0 → visited
1 → not visited
2 → not visited
3 → not visited
4 → not visited
```

---

# 🔥 9. BFS Starts

Now:

```java
while(!q.isEmpty())
```

Question:

> Is queue empty?

Queue:

```text
[0]
```

No.

Therefore:

```text
!q.isEmpty() = true
```

Enter the loop.

---

# 🟢 BFS — ITERATION 1

Execute:

```java
int node = q.poll();
```

`poll()` removes the first element.

Before:

```text
Queue = [0]
```

After:

```text
node = 0
Queue = []
```

---

## Check Destination

```java
if(node == destination)
```

We have:

```text
node = 0
destination = 4
```

Therefore:

```text
0 == 4
```

❌ False.

So continue.

---

# 🔷 Find Neighbors of Node 0

```java
for(int nei : list.get(node))
```

We know:

```text
list.get(0) = [1, 2]
```

Therefore:

```text
Neighbors of 0 = 1, 2
```

The `for` loop will execute twice.

---

## 🟡 Neighbor Iteration 1

```text
nei = 1
```

Check:

```java
if(!visit[nei])
```

We have:

```text
visit[1] = false
```

Therefore:

```text
!false = true
```

Enter the `if`.

### Mark node 1 visited

```java
visit[1] = true;
```

Now:

```text
visit = [true, true, false, false, false]
```

### Add node 1 to queue

```java
q.add(1);
```

Queue:

```text
[1]
```

---

## 🟡 Neighbor Iteration 2

Next neighbor:

```text
nei = 2
```

Check:

```java
visit[2]
```

Currently:

```text
false
```

Therefore:

```text
!false = true
```

Mark visited:

```java
visit[2] = true;
```

Now:

```text
visit = [true, true, true, false, false]
```

Add to queue:

```java
q.add(2);
```

Queue:

```text
[1, 2]
```

---

# 🔷 End of BFS Iteration 1

Current state:

```text
Processed node = 0

Queue:
[1, 2]

Visited:
[true, true, true, false, false]
```

Graph progress:

```text
        0 ✓
       / \
     1 ✓  2 ✓
      |    |
      3    4
```

Notice something important:

> We marked `1` and `2` visited **when adding them to the queue**, not when removing them.

This prevents the same node from being added multiple times.

---

# 🟢 BFS — ITERATION 2

Check:

```java
while(!q.isEmpty())
```

Queue:

```text
[1, 2]
```

Not empty.

Execute:

```java
int node = q.poll();
```

Remove first element:

```text
node = 1
```

Queue becomes:

```text
[2]
```

---

## Check Destination

```java
if(node == destination)
```

```text
1 == 4
```

❌ False.

Continue.

---

# 🔷 Find Neighbors of Node 1

```java
list.get(1)
```

gives:

```text
[0, 3]
```

So we have:

```text
nei = 0
nei = 3
```

---

## 🟡 Neighbor Iteration 1

```text
nei = 0
```

Check:

```text
visit[0]
```

It is:

```text
true
```

Therefore:

```java
if(!visit[0])
```

becomes:

```text
if(!true)
```

which is:

```text
false
```

So we **do nothing**.

We don't add `0` again.

---

## 🟡 Neighbor Iteration 2

```text
nei = 3
```

Check:

```text
visit[3]
```

Currently:

```text
false
```

Therefore:

```text
!false = true
```

Mark:

```java
visit[3] = true;
```

Now:

```text
visit = [true, true, true, true, false]
```

Add to queue:

```java
q.add(3);
```

Queue:

```text
[2, 3]
```

---

# 🔷 End of BFS Iteration 2

```text
Processed = 1

Queue = [2, 3]

Visited =
[true, true, true, true, false]
```

Graph:

```text
        0 ✓
       / \
     1 ✓  2 ✓
      |    |
      3 ✓  4
```

---

# 🟢 BFS — ITERATION 3

Queue:

```text
[2, 3]
```

Remove first:

```java
int node = q.poll();
```

Therefore:

```text
node = 2
```

Queue:

```text
[3]
```

---

## Check Destination

```text
node = 2
destination = 4
```

```text
2 == 4
```

❌ False.

Continue.

---

# 🔷 Find Neighbors of Node 2

```java
list.get(2)
```

gives:

```text
[0, 4]
```

So:

```text
nei = 0
nei = 4
```

---

## 🟡 Neighbor Iteration 1

```text
nei = 0
```

Check:

```text
visit[0] = true
```

So:

```text
!true = false
```

Do nothing.

---

## 🟡 Neighbor Iteration 2

```text
nei = 4
```

Check:

```text
visit[4] = false
```

Therefore:

```text
!false = true
```

Execute:

```java
visit[4] = true;
```

Now:

```text
visit = [true, true, true, true, true]
```

Then:

```java
q.add(4);
```

Queue:

```text
[3, 4]
```

---

# 🔷 End of BFS Iteration 3

```text
Processed = 2

Queue = [3, 4]

Visited =
[true, true, true, true, true]
```

Graph:

```text
        0 ✓
       / \
     1 ✓  2 ✓
      |    |
      3 ✓  4 ✓
```

We have now **found node 4**, but notice:

> We haven't returned `true` yet.

Why?

Because your code checks the destination when a node is **removed from the queue**:

```java
int node = q.poll();

if(node == destination)
    return true;
```

So `4` must come out of the queue.

---

# 🟢 BFS — ITERATION 4

Queue:

```text
[3, 4]
```

Execute:

```java
int node = q.poll();
```

First element is:

```text
node = 3
```

Queue becomes:

```text
[4]
```

---

## Check Destination

```text
node = 3
destination = 4
```

```text
3 == 4
```

❌ False.

Continue.

---

# 🔷 Find Neighbors of Node 3

```java
list.get(3)
```

gives:

```text
[1]
```

So:

```text
nei = 1
```

Check:

```text
visit[1] = true
```

Therefore:

```text
!true = false
```

Do nothing.

End of iteration.

---

# 🟢 BFS — ITERATION 5

Queue:

```text
[4]
```

Not empty.

Execute:

```java
int node = q.poll();
```

Therefore:

```text
node = 4
```

Queue:

```text
[]
```

---

# 🔥 Check Destination

Now:

```java
if(node == destination)
```

We have:

```text
node = 4
destination = 4
```

Therefore:

```text
4 == 4
```

✅ TRUE!

So:

```java
return true;
```

The method immediately stops.

