# ☕ Wrapper Classes in Java
<img width="1024" height="1536" alt="image" src="https://github.com/user-attachments/assets/77a7888f-d0e7-4e7c-8862-45e6f9d65b40" />

## 📌 What is a Wrapper Class?

A Wrapper Class converts a **primitive data type into an object**.

Java provides a wrapper class for each primitive type.

👉 Primitive → Object

---

# Why Do We Need Wrapper Classes?

* Collections (ArrayList, HashMap) store objects, not primitives.
* Provides useful methods.
* Supports null values.
* Required for Generics.

---

# 📊 Primitive Types and Wrapper Classes

| Primitive Type | Wrapper Class |
| -------------- | ------------- |
| byte           | Byte          |
| short          | Short         |
| int            | Integer       |
| long           | Long          |
| float          | Float         |
| double         | Double        |
| char           | Character     |
| boolean        | Boolean       |

---

# Example

```java
public class WrapperDemo {
    public static void main(String[] args) {

        int num = 100;          // Primitive

        Integer obj = 100;      // Wrapper Object

        System.out.println(num);
        System.out.println(obj);
    }
}
```

### Output

```text
100
100
```

---

# 1️⃣ Autoboxing

Automatic conversion of a primitive type into a wrapper object.

### Example

```java
public class AutoBoxingDemo {
    public static void main(String[] args) {

        int num = 10;

        Integer obj = num;   // Autoboxing

        System.out.println(obj);
    }
}
```

### Behind the Scenes

```java
Integer obj = Integer.valueOf(num);
```

---

# 2️⃣ Unboxing

Automatic conversion of a wrapper object into a primitive type.

### Example

```java
public class UnboxingDemo {
    public static void main(String[] args) {

        Integer obj = 20;

        int num = obj;      // Unboxing

        System.out.println(num);
    }
}
```

### Behind the Scenes

```java
int num = obj.intValue();
```

---

# Common Wrapper Class Methods

## Integer

```java
public class IntegerMethods {
    public static void main(String[] args) {

        String str = "123";

        int num = Integer.parseInt(str);

        System.out.println(num);

        System.out.println(Integer.MAX_VALUE);
        System.out.println(Integer.MIN_VALUE);
    }
}
```



# Wrapper Classes with Collections

❌ Not Allowed

```java
ArrayList<int> list = new ArrayList<>();
```

✅ Allowed

```java
import java.util.*;

public class WrapperCollection {
    public static void main(String[] args) {

        ArrayList<Integer> list = new ArrayList<>();

        list.add(10);
        list.add(20);

        System.out.println(list);
    }
}
```

---



}
```

